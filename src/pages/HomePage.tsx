import React from "react";

interface HomeProps {
  // Define any props that the component will accept
}

const HomePage: React.FC<HomeProps> = () => {
  // Define any state variables or functions that the component will use

  return (
    // Define the JSX that the component will render
    <div>
      <h1>Home</h1>
    </div>
  );
};

export default HomePage;
